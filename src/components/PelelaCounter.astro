<section class="pelela-demo" aria-label="Demo interactiva de PelelaJS">
  <div id="pelela-counter-root"></div>
</section>

<script>
  import { mountTemplate } from 'pelelajs'
  import template from '../app.pelela'
  import 'virtual:pelela-auto-register'

  const root = document.getElementById('pelela-counter-root')

  if (root) {
    mountTemplate(root, template)
  }
</script>

<style>
  .pelela-demo {
    /* Starlight aplica márgenes a los bloques Markdown; el componente controla el suyo. */
    margin: 1.25rem 0 !important;
    border: 4px solid var(--pelela-demo-border, #151515);
    background: var(--pelela-demo-bg, #fff3d6);
    box-shadow: 8px 8px 0 var(--pelela-demo-shadow, #151515);
    padding: 1rem;
  }

  .pelela-demo :global(.pelela-counter) {
    display: grid;
    gap: 0.75rem;
    justify-items: center;
    color: var(--pelela-demo-text, #151515);
    font-family: Inter, ui-sans-serif, system-ui, sans-serif;
  }

  .pelela-demo :global(strong) {
    color: var(--pelela-demo-text, #151515);
    font-size: 1.05rem;
    letter-spacing: -0.02em;
  }

  .pelela-demo :global(.counter-row) {
    display: flex;
    align-items: center;
    gap: 0.9rem;
  }

  .pelela-demo :global(button) {
    width: 3.5rem;
    height: 3.5rem;
    border: 3px solid var(--pelela-demo-shadow, #151515);
    background: #5c9dff;
    box-shadow: 2px 2px 0 var(--pelela-demo-shadow, #151515);
    color: #151515;
    cursor: pointer;
    font: inherit;
    font-weight: 900;
    padding: 0;
  }

  .pelela-demo :global(p) {
    width: 4rem;
    height: 4rem;
    margin: 0;
    border: 3px solid var(--pelela-demo-shadow, #151515);
    background: #ffd84d;
    box-shadow: 2px 2px 0 var(--pelela-demo-shadow, #151515);
    font-size: 2rem;
    font-weight: 950;
    line-height: 1;
    padding: 0.5rem 0.75rem;
    text-align: center;
  }
</style>
